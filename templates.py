# templates.py
# コード生成AI用のテンプレート管理ファイル
# 今後テンプレートを追加していく中心ファイル

TEMPLATES = {

    # -------------------------
    # ① 累積和テンプレート
    # -------------------------
    "累積和": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
累積和（1からNまでの値を順に足し、途中経過を出力する問題）

【厳守するコード仕様】
- 変数 total を 0 で初期化する
- 1 から N までループする
- 各ステップで total に現在の値を加算する
- 各ステップで「現在の値」と「現在の total」を出力する
- 最後に total を出力する
- 余計な処理は行わない
- 問題文に N が明記されていない場合は、問題文から推論する

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
""",

    # -------------------------
    # ② 条件分岐テンプレート
    # -------------------------
    "条件分岐": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
条件分岐（if / elif / else を用いて条件に応じた処理を行う問題）

【厳守するコード仕様】
- 問題文に書かれた条件を正確に読み取り、if / elif / else を使って実装する
- 条件は重複なく網羅的に扱うこと
- 出力形式は問題文の指示に従うこと
- 余計な処理や追加の出力は行わないこと
- 入力値が複数ある場合は、問題文から推論して適切に変数を設定すること

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
""",

    # -------------------------
    # ③ 配列処理テンプレート
    # -------------------------
    "配列処理": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
配列処理（リスト・配列の要素を走査・計算・抽出・変換する問題）

【厳守するコード仕様】
- 配列（リスト）をループで処理すること
- 問題文に書かれた処理内容（最大値・最小値・合計・抽出・変換など）を正確に実装すること
- 出力形式は問題文の指示に従うこと
- 余計な処理や追加の出力は行わないこと
- 入力形式が曖昧な場合は、問題文から推論して適切に配列を定義すること
- ソートが必要な場合は、言語標準のソート関数を使用してよい

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
""",

    # -------------------------
    # ④ 文字列処理テンプレート
    # -------------------------
    "文字列処理": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
文字列処理（文字列の検索・置換・判定・変換などを行う問題）

【厳守するコード仕様】
- 文字列を正しく扱い、問題文に書かれた処理内容（検索・置換・判定・変換など）を正確に実装すること
- 必要に応じて文字列を1文字ずつループで処理してよい
- 標準ライブラリの文字列関数（length, replace, find, split など）は使用してよい
- 出力形式は問題文の指示に従うこと
- 余計な処理や追加の出力は行わないこと
- 入力形式が曖昧な場合は、問題文から推論して適切に文字列を定義すること

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
""",

    # -------------------------
    # ⑤ 数学系テンプレート
    # -------------------------
    "数学系": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
数学系（素数判定・最大公約数・最小公倍数・約数列挙・桁操作・数列計算など）

【厳守するコード仕様】
- 問題文に書かれた数学的処理（素数判定・GCD・LCM・約数列挙・桁操作など）を正確に実装すること
- 必要に応じてループ・再帰・標準ライブラリを使用してよい
- 数学的定義に基づいた正しいアルゴリズムを使用すること
- 出力形式は問題文の指示に従うこと
- 余計な処理や追加の出力は行わないこと
- 入力形式が曖昧な場合は、問題文から推論して適切に変数を設定すること

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
""",

    # -------------------------
    # ⑥ 探索（DFS/BFS)テンプレート
    # -------------------------
    "探索": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
探索（DFS / BFS を用いてグラフ・木・迷路などを探索する問題）

【厳守するコード仕様】
- 問題文に書かれた探索方法（DFS または BFS）を正確に実装すること
- DFS の場合は再帰またはスタック、BFS の場合はキューを使用すること
- 訪問済み管理（visited 配列や集合）を必ず行うこと
- グラフ・木・迷路などの入力形式が曖昧な場合は、問題文から推論して適切に定義すること
- 出力形式は問題文の指示に従うこと
- 余計な処理や追加の出力は行わないこと

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
""",

    # -------------------------
    # 一般（fallback)テンプレート
    # -------------------------
    "一般": """
あなたは競技プログラミングのプロです。
次の問題を解く {language} のコードを生成してください。

【問題タイプ】
一般（特定のカテゴリに分類できない問題）

【厳守するコード仕様】
- 問題文の内容を正確に読み取り、必要な処理をそのまま実装すること
- 必要に応じてループ・条件分岐・配列処理・文字列処理などを組み合わせてよい
- 特定のアルゴリズムを強制しないが、問題文の意図に沿った最適な方法を選択すること
- 出力形式は問題文の指示に従うこと
- 余計な処理や追加の出力は行わないこと
- 入力形式が曖昧な場合は、問題文から推論して適切に変数を設定すること

【出力形式】
[CODE]
<ここにコードのみを書く>

[EXPLANATION]
<ここに説明文を書く。最後に必ず計算量（Big-O）を書く>

【禁止事項】
- タグ名（[CODE], [EXPLANATION]）を変更しない
- 閉じタグ（[/CODE], [/EXPLANATION]）を出力しない
- コードブロック（```）を使わない
- コードの前に説明文を書かない
- 説明文の中にコードを書かない

問題:
{problem_text}
"""
}
